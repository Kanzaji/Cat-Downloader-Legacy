#!/bin/sh
# This script is used to configure and run CatDownloader!
# You can add few additional arguments to change the behaviour of the app.
# Available arguments:
# -Mode: / -WorkingDirectory: / -ThreadCount: / -DownloadAttempts: / -SizeVerification: / -SumCheckVerification / -Logger
# Fore more information, check out the github repo of this app! https://github.com/Kanzaji/Cat-Downloader-Legacy

# You can configure the command here:
java -jar Cat-Downloader-Legacy-1.0.jar